import React from 'react';
import './Filter.module.scss';

const Filter = () => {
    return (
        <form>
            <input type="text" /><br/>
            <button>Search</button>
        </form>
    );
};

export default Filter;
